<% form_for [@commentable, Comment.new] do |f| %>
  <p>
    <%= f.label :content %><br />
    <%= f.text_area :content %>
  </p>
   <%= f.hidden_field :user_id, :value => @current_user.id %>
  <p><%= f.submit "Submit" %></p>
<% end %>

