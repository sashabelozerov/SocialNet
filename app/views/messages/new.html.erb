<% "New Message" %>

<% form_for :message, :url => user_messages_path(@current_user, :mailbox => "sent") do |f| %>
  <%= f.error_messages %>
  <p>
    <%= f.label :title %><br />
    <%= f.text_field :title %>
  </p>
  <p>
    <%= f.label :content %><br />
    <%= f.text_area :content %>
  </p>
  <p>
	<%= f.label :user_id_target %>
	<%= f.text_field :user_id_target %>
  </p>
  <p><%= f.submit "Submit" %></p>
<% end %>
